#!/bin/bash
# Version Manager Extension (opt-in)
#
# PBR defaults to system packages via pacman. If a machine needs
# language version managers (nvm, pyenv, rbenv, etc.), copy this file:
#
#   cp extensions/version-managers.sh.example extensions/version-managers.sh
#
# Then uncomment the sections you need. The extension auto-loads on next shell start.

# ── Node.js (nvm) ───────────────────────────────────────
# export NVM_DIR="$HOME/.nvm"
# if [[ -s /usr/share/nvm/init-nvm.sh ]]; then
#   source /usr/share/nvm/init-nvm.sh
# elif [[ -s "$NVM_DIR/nvm.sh" ]]; then
#   source "$NVM_DIR/nvm.sh"
# fi

# ── Python (pyenv) ──────────────────────────────────────
# export PYENV_ROOT="$HOME/.pyenv"
# export PATH="$PYENV_ROOT/bin:$PATH"
# if command -v pyenv >/dev/null 2>&1; then
#   eval "$(pyenv init -)"
# fi

# ── Ruby (rbenv) ────────────────────────────────────────
# if command -v rbenv &>/dev/null; then
#   eval "$(rbenv init -)"
# fi

# ── Bun ─────────────────────────────────────────────────
# export BUN_INSTALL="$HOME/.bun"
# export PATH="$BUN_INSTALL/bin:$PATH"
# [ -s "$BUN_INSTALL/_bun" ] && source "$BUN_INSTALL/_bun"

# ── pnpm ────────────────────────────────────────────────
# export PNPM_HOME="$HOME/.local/share/pnpm"
# case ":$PATH:" in
# *":$PNPM_HOME:"*) ;;
# *) export PATH="$PNPM_HOME:$PATH" ;;
# esac

# ── Go ──────────────────────────────────────────────────
# if [[ "$PBR_PLATFORM" == "macos" ]]; then
#   export GOROOT=$(brew --prefix go)/libexec
# else
#   export GOROOT=/usr/lib/go
# fi
# export GOPATH=$HOME/go
# export PATH=$GOPATH/bin:$GOROOT/bin:$PATH

# ── Android SDK ─────────────────────────────────────────
# if [[ "$PBR_PLATFORM" == "macos" ]]; then
#   export ANDROID_HOME=$HOME/Library/Android/sdk
#   export JAVA_HOME=/opt/homebrew/opt/openjdk@17
# else
#   export ANDROID_HOME=/opt/android-sdk
#   export JAVA_HOME=/usr/lib/jvm/java-17-openjdk
# fi
# export PATH=$JAVA_HOME/bin:$PATH
# export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin
# export PATH=$PATH:$ANDROID_HOME/platform-tools
# export PATH=$PATH:$ANDROID_HOME/emulator
