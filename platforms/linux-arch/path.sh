#!/bin/bash
# Arch Linux PATH — only system-level paths.
# Version manager and Android SDK paths are opt-in via extensions.

export PATH="$HOME/.local/bin:$PATH"
export PATH="$HOME/.docker/bin:$PATH"
